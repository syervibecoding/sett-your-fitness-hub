# Agent Instructions

> 📨 **CODEX: read `HANDOFF-CLAUDE-TO-CODEX.md` first.** Claude Code committed fixes (`2b326c3`) and needs you to handle **C3** (add `ProtectedRoute` + ownership check to `/aluno/treino/:studentId` in `src/App.tsx`). Do NOT edit the files Claude owns — see the handoff + `CLAUDE.md` "Multi-Agent Live Coordination".

Codex and other coding agents should follow `CLAUDE.md` for this repository. Keep local credentials in `.env.local`, avoid committing secrets, and run the relevant project checks before handing off work.
