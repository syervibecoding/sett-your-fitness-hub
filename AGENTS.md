# Agent Instructions

> 📨 **CODEX — START HERE: read `CODEX-IMPLEMENT-AND-UNIFY.md`.** Claude Code shipped a large CX + design + security pass this session (15+ `claude:` commits). The Matheus asked you to **integrate it, unify the editorial design system across the whole project, and implement 4 remaining features** (anamnese multi-step, progress photos, automation triggers, abandoned-cart). After you finish and Matheus reviews, **Claude deploys the edge functions.** Still open for you: **C3** (`ProtectedRoute` on `/aluno/treino/:studentId` in `src/App.tsx`) and the broken `Bebas Neue` font in your files. Do NOT edit Claude-owned files — see `CLAUDE.md` "Multi-Agent Live Coordination" + `HANDOFF-CLAUDE-TO-CODEX.md`.

Codex and other coding agents should follow `CLAUDE.md` for this repository. Keep local credentials in `.env.local`, avoid committing secrets, and run the relevant project checks before handing off work.
